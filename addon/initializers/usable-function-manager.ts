import 'ember-could-get-used-to-this/-private/functions';

/**
 * This initializer doesn't actually do anything and just makes sure that some
 * private code gets loaded while the application boots.
 */
export function initialize(/* application */): void {
  // application.inject('route', 'foo', 'service:foo');
}

export default {
  initialize,
};
